class Aircraft:

    def __init__(self, callsign, current_node, current_edge, distance_on_edge, speed, current_state):
        self.callsign=callsign
        self.current_node=''
        self.current_edge=''
        distance_on_edge=-1
        speed=0
        current_state='parked'

    